const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

  message.channel.send("We are currently working on a forum that we will hopefully release before the server so the hype can be released on there! If you would like to save the address before it is released then feel free! https://forum.questuniverse.tk");

}

module.exports.help = {
  name: "forum"
}
